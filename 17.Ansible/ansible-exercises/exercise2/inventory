all:
  hosts:
    web01:
      ansible_host: 172.31.31.178
      ansible_user: ec2-user
      ansible_ssh_private_key_file: clientkey.pem
    web02:
      ansible_host: 172.31.22.225
      ansible_user: ec2-user
      ansible_ssh_private_key_file: clientkey.pem
    db01:
      ansible_host: 172.31.19.215
      ansible_user: ec2-user
      ansible_ssh_private_key_file: clientkey.pem


# Grouping the hosts into respective server roles and data center location
  children: 
    webservers: # Defining webservers group   
      hosts:  # Defining hosts under webservers group
        web01: 
        web02:
    dbservers: # Defining dbservers group
      hosts: # Defining hosts under dbservers group
        db01:
# grouping the grops
    dc_oregon: # defining the parent group 
      children:
        webservers:
        dbservers:
